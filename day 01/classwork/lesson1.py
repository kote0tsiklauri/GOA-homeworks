#name = "nika"
#name არის ცვლადი
# = არის ცვლადისთვის მნიშვნელობის მიმნიჭებელი სიმბოლო
# "nika" არის ცვლადისთვის მნიშვნელობა

surname = "keselava"

#printe(name)
#პრინტ ფუნქციას გადეცემა ეკრანზე გამოსატანი ობიეტი

name = "nika" # ეს არს str (string) ტიპის ცვლადი
age = 22 #ეს არის int (integer) მთელი რიცხვი
height = 184.5 #ეს არის float ტიპის ცვლადი (ათწილადი)
#boolean (bool) ტიპის ცვლადი

knows_programing = true  # true ან false
is_ugly = false # snakecase (უბრალოდ წერის სტილი სრანდარტულად)

print(name + " " + surname)

print(name + " " + str(age))