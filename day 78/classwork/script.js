function getElementLengths(matrix) {
    let result = [];
    for (let i = 0; i < matrix.length; i++) {
        let row = matrix[i];
        for (let j = 0; j < row.length; j++) {
            
        }
    }
    return result;
}

const matrix = [
    ['hello', 'world'],
    [123, 'array', 'inside', 'test']
];

console.log(getElementLengths(matrix));